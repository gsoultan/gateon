# This file ensures the internal/ui/dist directory always exists so the
# //go:embed all:dist directive in ui.go compiles even before the frontend
# is built (e.g. in CI lint/staticcheck jobs). The real build artifacts are
# generated by `bun run build` and remain gitignored.
